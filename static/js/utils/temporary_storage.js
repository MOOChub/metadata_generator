/**
 * Stores all configuration data which is received from the server.
 *
 * @type {Map<string, any>} - A map containing all configuration information.
 */
export const framework_configs = new Map();

/**
 * Stores all search results to a query.
 *
 * @type {Map<string, any>} - A map with all search results to a query grouped by the corresponding frameworks.
 */
export const search_results = new Map();

/**
 * Stores all selected entries.
 *
 * @type {Map<string, any>} - A map containing all selected entries grouped by the corresponding frameworks.
 */
export const all_selected = new Map();